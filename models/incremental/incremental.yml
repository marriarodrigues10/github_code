version: 2
sources:
    - name: datafeed_shared_schema
      database: "{{('PROD')}}"
      schema: DBT_DEV
      tables:
        - name: CUSTOMERS
          columns:
          - name: ID
            description: "The primary key for this table"
            tests:
              - unique 
        - name: CUSTOMERS2
          columns:
          - name: ID
            description: "The primary key for this table"
            tests:
              - relationships:
                  to: 'prod.dbt_dev.INSERT_OVERWRITE'
                  field: ID 


        - name: customers_snaps
        - name: emp_tax