version: 2
sources:
    - name: datafeed_shared_schema
      database: "{{('PROD')}}"
      schema: DBT_DEV
      tables:
        - name: CUSTOMERS
        - name: CUSTOMERS2
        - name: customers_snaps
        